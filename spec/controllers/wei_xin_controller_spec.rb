require 'spec_helper'

describe WeiXinController do

  describe :query do
    it 'ensure can parse xml request and get message type' do
      #lambda do
      #  xml_text_message = File.read('text_message.xml')
      #  post :query, xml_text_message, :content_type => 'application/xml'
      #end
      #response.body.should include('text')
    end
  end
end
